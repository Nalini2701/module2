def test_knapsack():
    prices = [100, 200, 300]
    returns = [10, 20, 30]
    capacity = 500
    dp = knapsack(prices, returns, capacity)
    # Add assertions to verify the correctness of dp table
